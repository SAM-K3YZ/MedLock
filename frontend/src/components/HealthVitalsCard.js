import { Text, View } from 'react-native';

function HealthVitalsCard({ title, iconName, readings, unit }) {
  <View>
    <Text>Health Vital Card</Text>
  </View>;
}

export default HealthVitalsCard;
