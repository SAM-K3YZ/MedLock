import { SolidColor, GradientColor } from "./color";
import { FONTS, FONT_SIZES } from "./fonts";
import { SIZES } from "./sizes";

export const theme = {
  SolidColor,
  GradientColor,
  FONTS,
  FONT_SIZES,
  SIZES,
};

export default theme;
